{
  "exp_name": "fc0_sample",

  "config_induce": {
    "pseudo_init_method": "sample",

    "num_classes": 10,
    "num_training_steps": 500000,
    "num_burnin_steps": 1000,

    "data_numpy_pkl": "./data/mnist/data_numpy.pkl",

    "batch_size": 1000,

    "ensemble_dim": 10,
    "layer_dims": [784, 10],

    "train_data_size": 50000,
    "pseudo_data_size": 100,

    "walk_size": 1e-5,
    "fisher_learning_rate": 1e-2,

    "num_walks_per_step": 1,

    "max_to_keep": 2
  },

  "config_cla": {
    "num_classes": 10,
    "num_training_steps": 5000,

    "batch_size": 64,

    "ensemble_dim": 10,
    "layer_dims": [784, 10],

    "train_data_size": 50000,

    "walk_size": 1e-5,

    "num_walks_per_step": 1,

    "max_to_keep": 2
  },

  "num_steps_per_iter": 5000
}
